{
  "name": "mcp-question-server",
  "version": "1.0.0",
  "description": "Minimal Node.js MCP server using Express and Mongoose for fetching questions by topic and difficulty.",
  "main": "src/server.js",
  "license": "MIT",
  "scripts": {
    "build": "npm ci || npm install",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node seed/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
